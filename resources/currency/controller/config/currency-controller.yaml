apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: currency-controller
spec:
  template:
    spec:
      containers:
      - image: gcr.io/rikudog/currency-controller:v1
        imagePullPolicy: Always